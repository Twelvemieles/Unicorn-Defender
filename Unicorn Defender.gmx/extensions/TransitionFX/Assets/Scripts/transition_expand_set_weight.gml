///transition_expand_set_weight(transition, weightx, weighty)

// This applies a weight to the animation, leading the expand box from
// the side of the screen

argument0.weightx = argument1;
argument0.weighty = argument2;
