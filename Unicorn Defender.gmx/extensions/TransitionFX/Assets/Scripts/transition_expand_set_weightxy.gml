///transition_expand_set_weightxy(transition, x, y)

// Same as the previous weight, only using x/y coordinates

argument0.weightx = argument1 / view_wview;
argument0.weighty = argument2 / view_hview;
